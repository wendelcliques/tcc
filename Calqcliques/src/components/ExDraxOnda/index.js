import React from 'react'
import { View, Text } from 'react-native'

const ExDraxOnda = () => {
    return (
        <View>
            <Text>Derive a função:</Text>
            <Text>Se for função composta considerar v(u) a função externa e u(x) a função interna.</Text>
                <Text>G(x) = sen(x²)</Text>
        </View>
    )
}

export default ExDraxOnda
